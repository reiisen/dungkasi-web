---
import HeaderLink from "./HeaderLink.astro";
import { SITE_TITLE } from "../consts";
---

<header>
  <nav>
    <!--<div class="social-links">-->
    <!--  <a href="/" target="_blank">-->
    <!--    <span class="sr-only">Follow Astro on Mastodon</span>-->
    <!--    <svg viewBox="0 0 16 16" aria-hidden="true" width="32" height="32"-->
    <!--      ><path-->
    <!--        fill="currentColor"-->
    <!--        d="M1 6V15H6V11C6 9.89543 6.89543 9 8 9C9.10457 9 10 9.89543 10 11V15H15V6L8 0L1 6Z"-->
    <!--      ></path></svg-->
    <!--    >-->
    <!--  </a>-->
    <!--</div>-->
    <!-- This div will be centered independently -->
    <h2 class="disable">{SITE_TITLE}</h2>
    <div class="internal-links">
      <HeaderLink href="/">Home</HeaderLink>
      <HeaderLink href="/berita">Berita</HeaderLink>
      <HeaderLink href="/lokasi">Lokasi</HeaderLink>
    </div>
    <div class="social-links">
      <a href="https://instagram.com/kim_dungkasingipak" target="_blank">
        <span class="sr-only">Follow us on Instagram</span>
        <svg viewBox="0 0 24 24" aria-hidden="true" width="32" height="32">
          <!-- The rounded square outline -->
          <rect
            x="1.5"
            y="1.5"
            width="21"
            height="21"
            rx="3.82"
            fill="none"
            stroke="currentColor"
            stroke-width="3"></rect>
          <!-- The circle outline in the middle -->
          <circle
            cx="12"
            cy="12"
            r="4.77"
            fill="none"
            stroke="currentColor"
            stroke-width="3"></circle>
          <!-- The solid dot in the corner -->
          <circle cx="18.2" cy="5.8" r="1.8" fill="currentColor"></circle>
          <!-- Radius increased for balance -->
        </svg>
      </a>
    </div>
  </nav>
</header>
<style>
  header {
    margin: 0;
    padding: 0 1em;
    background: white;

    /* ADD THESE THREE LINES */
    position: sticky;
    top: 0;
    z-index: 10; /* Ensures the header stays on top of other content */
  }
  h2 {
    margin: 0;
    font-size: 1em;
  }

  h2 a,
  h2 a.active {
    text-decoration: none;
  }
  nav {
    /* MODIFIED: Add position relative to act as a container for the absolute child */
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  nav a {
    padding: 1em 0.5em;
    color: var(--black);
    border-bottom: 4px solid transparent;
    text-decoration: none;
  }
  nav a.active {
    text-decoration: none;
    border-bottom-color: var(--accent);
  }

  /* ADDED: Style for the centered links */
  .internal-links {
    position: absolute;
    /* Center it horizontally and vertically */
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    /* Ensure its own children (the links) are laid out with flex */
    display: flex;
  }

  .social-links,
  .social-links a {
    display: flex;
  }
  @media (max-width: 720px) {
    .social-links {
      display: none;
    }
    .disable {
      display: none;
    }
    /* ADDED: On small screens, ensure internal links are still centered correctly */
    nav {
      justify-content: center;
    }
    .internal-links {
      position: static; /* Let it return to the normal flow */
      transform: none; /* Reset the transform */
    }
  }
</style>
